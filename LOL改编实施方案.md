# LoL 英雄主题改编实施方案

> 状态：**阶段 A~G 已全部实施完成**，并已完成第二轮优化（平衡调优 + 视觉重做 + 打击表现 + 体验优化）。本文件保留为需求与规格来源。
>
> 实施结果摘要：
> - 20 名英雄 + 英雄复制器、5 地区 6 职业 2/4 羁绊、法力/技能/护盾/治疗/控制/多段攻击全部实现。
> - 新增 `core/targeting.ts`、`core/traits.ts`、`config/traits.ts`、`config/balance.ts`；重写 `core/battle.ts`、`core/effects.ts`、`bot/random.ts`、`web/ui.ts`、`web/style.css`。
> - 21 个测试文件 / 218 个测试通过，`npx tsc --noEmit` 无错，`npm run web:build` 成功。
> - 1000 局模拟通过：平均 10.4 回合、2.3ms/局、无崩溃、无 NaN、名次完整；并输出每个英雄/羁绊的"前四率 lift"作为平衡依据。
> - 资源：`scripts/fetch-lol-assets.mjs` 已下载 20 张官方 Data Dragon 头像（版本 16.18.1 / zh_CN），清单 `web/public/assets/lol/manifest.json` 记录真实 SHA-256。
> - 数值与平衡基线见 `当前数值.md`；第 5 节伤害曲线的 K 由 40 调到 66（阶段 G 调优，公式形状未变），原因与锚点已记录在 `config/damage.ts`。
> - 第二轮优化内容：参考同类自走棋手游的信息层级重做 UI（八边形棋盘 / 金色按钮 / 角上大红大绿数值 / 羁绊徽标 / 卡牌详情 / 商店概率表 / 对手阵容预览），
>   战斗加入受击闪光、震屏、大号伤害数字、技能横幅、切后排与时空回溯特效、击杀爆点；
>   平衡上只改有数据支持的项（补强艾欧尼亚/射手/德莱文/亚索/慎/德莱厄斯），并回滚了无数据支持的法师/刺客削弱。
>
> 基础项目：悠悠牌 8 人自动战斗卡牌 MVP
> 当前目标：制作可在本地完整游玩的 LoL 英雄主题原型，而不是只替换角色名称

---

## 1. 项目定位

保留当前项目已经验证稳定的部分：

- 8 名玩家同局（1 名玩家 + 7 名 AI）
- 商店购买、刷新、升级与金币经济
- 2×3 棋盘、前排与后排站位
- 三张同名英雄合成为 2 星英雄
- 自动战斗、回合扣血、淘汰排名
- 确定性随机、战斗事件日志、Web 与 CLI

改造成具有 LoL 辨识度的版本：

- 英雄具有地区、职业和一个标志性技能
- 英雄通过普攻和受伤积攒法力，满法力后施放技能
- 地区与职业组成 2/4 人羁绊
- 支持护盾、治疗、控制、多段伤害、后排攻击、召唤和死亡效果
- 卡面显示头像、地区、职业、星级、攻击、生命和法力
- 战斗界面能表现技能释放、护盾、治疗、眩晕和暴击等事件

第一版定位为本地个人试玩原型。若以后公开发布或商业化，需要在发布前单独完成 IP、名称、角色形象、图标、音效和商标授权检查；未获得合适许可时，应替换为原创的 MOBA 风格角色与资源。

---

## 2. 第一版范围

### 2.1 必须完成

1. 20 名英雄的完整卡池。
2. 5 个地区羁绊与 6 个职业羁绊。
3. 每名英雄 1 个核心技能。
4. 法力、技能释放、护盾、治疗、眩晕、增益和减益。
5. 目标选择：前排、后排、最低生命、随机目标和友方目标。
6. 2 星合成后属性与技能数值成长。
7. 能理解英雄价值、羁绊和站位的基础 AI。
8. 新卡面、新商店、新棋盘和战斗技能表现。
9. 为新机制补充单元测试、确定性测试和完整对局测试。

### 2.2 第一版暂不做

- 真人联网与账号系统
- 排位、赛季、通行证和付费系统
- 装备合成树与完整 LoL 装备库
- 英雄皮肤系统
- 三维模型与复杂骨骼动画
- 超过 20 名英雄的大型卡池
- 直接发布到应用商店

装备系统放到第二版，避免英雄、羁绊、法力和装备同时进入后难以平衡。

---

## 3. 首发英雄池

首发使用 5 个地区，每个地区 4 名英雄，共 20 名。英雄稀有度会影响价格和出现的商店等级，但不会限制羁绊组合。

| 地区 | 英雄 | 职业 | 建议品质 | 核心玩法 |
|---|---|---|---|---|
| 德玛西亚 | 盖伦 | 先锋/战士 | 绿 | 旋转攻击全体敌人并获得护盾 |
| 德玛西亚 | 波比 | 先锋 | 蓝 | 护盾并眩晕当前目标 |
| 德玛西亚 | 拉克丝 | 法师/辅助 | 紫 | 攻击后排并为低血友方提供护盾 |
| 德玛西亚 | 嘉文四世 | 战士/辅助 | 橙 | 跳向敌阵并为友方提供护盾 |
| 诺克萨斯 | 德莱厄斯 | 战士 | 绿 | 低血目标斩杀，击杀后再次行动 |
| 诺克萨斯 | 德莱文 | 射手 | 蓝 | 连续投掷飞斧攻击后排 |
| 诺克萨斯 | 卡特琳娜 | 刺客 | 紫 | 跳后排并造成多段范围伤害 |
| 诺克萨斯 | 斯维因 | 法师 | 橙 | 范围伤害并按伤害回复生命 |
| 艾欧尼亚 | 慎 | 先锋/辅助 | 绿 | 为最低生命友方施加护盾 |
| 艾欧尼亚 | 亚索 | 战士/刺客 | 蓝 | 连续斩击，第三次攻击击飞目标 |
| 艾欧尼亚 | 阿狸 | 法师 | 紫 | 法术伤害并短暂魅惑目标 |
| 艾欧尼亚 | 艾瑞莉娅 | 战士/刺客 | 橙 | 在多个低生命敌人之间连续突进 |
| 皮尔特沃夫/祖安 | 蔚 | 先锋/战士 | 绿 | 击破护盾并眩晕前排目标 |
| 皮尔特沃夫/祖安 | 金克丝 | 射手 | 蓝 | 击杀后获得额外攻击并攻击后排 |
| 皮尔特沃夫/祖安 | 凯特琳 | 射手 | 紫 | 优先狙击后排最低生命目标 |
| 皮尔特沃夫/祖安 | 艾克 | 刺客/法师 | 橙 | 记录生命，受致命伤时回溯一次 |
| 暗影岛 | 赫卡里姆 | 先锋/战士 | 绿 | 冲锋攻击一列敌人 |
| 暗影岛 | 卡莉丝塔 | 射手 | 蓝 | 叠加长矛并在阈值时引爆 |
| 暗影岛 | 锤石 | 辅助 | 紫 | 为最低生命友方提供护盾并拉扯目标 |
| 暗影岛 | 佛耶戈 | 刺客 | 橙 | 击杀后短暂继承目标部分攻击力 |

另保留一张非英雄功能牌“英雄复制器”，承担当前“分裂者”的万能合成功能，不参与地区和职业羁绊。

以上是已确认的实施名单，不再在编码前重新询问。每名英雄有 1 个地区、1～2 个职业；英雄复制器没有地区或职业。

---

## 4. 羁绊方案

### 4.1 地区羁绊

| 地区 | 2 人效果 | 4 人效果 |
|---|---|---|
| 德玛西亚 | 战斗开始时全队获得少量护盾 | 护盾提高，首次失去护盾时获得攻击力 |
| 诺克萨斯 | 击杀后永久获得本场攻击力 | 攻击成长提高，并回复少量生命 |
| 艾欧尼亚 | 全队获得额外初始法力 | 施放技能后返还部分法力 |
| 皮尔特沃夫/祖安 | 每场随机强化一名友方 | 强化两名友方，并提高技能数值 |
| 暗影岛 | 首名友方阵亡时强化其余友方 | 每次友方阵亡都为存活友方提供灵魂层数 |

### 4.2 职业羁绊

| 职业 | 2 人效果 | 4 人效果 |
|---|---|---|
| 先锋 | 增加最大生命 | 额外获得战斗开始护盾 |
| 战士 | 增加攻击力 | 低生命时获得伤害减免 |
| 法师 | 提高技能伤害/治疗 | 首次施法后返还法力 |
| 射手 | 普攻有概率额外攻击一次 | 额外攻击概率与伤害提高 |
| 刺客 | 战斗开始跳向后排 | 首次技能必定强化 |
| 辅助 | 治疗和护盾效果提高 | 增益同时影响额外一名友方 |

羁绊数值全部进入独立配置文件，首轮以可玩性为主，之后通过批量 AI 对局统计调整。

---

## 5. 战斗规则改造

### 5.1 基础属性

英雄实例新增：

- `maxHp`：最大生命
- `mana`：当前法力
- `maxMana`：技能所需法力
- `startMana`：开战初始法力
- `shield`：当前护盾
- `status`：眩晕、增伤、减伤、标记等状态
- `traits`：地区与职业
- `targeting`：技能与普攻目标规则

### 5.2 法力与技能

- 普攻后获得固定法力。
- 受到有效伤害后获得少量法力。
- 行动开始时若法力已满，优先施放技能而不是普攻。
- 施法后法力归零，技能产出独立战斗事件。
- 2 星英雄提高基础攻血，并按配置提高技能数值；不简单地把所有技能翻倍。

### 5.3 状态结算顺序

单个英雄的行动顺序固定为：

1. 处理行动前状态。
2. 若眩晕则跳过行动并减少持续回合。
3. 若法力已满则施放技能，否则普通攻击。
4. 结算伤害、护盾、治疗和死亡。
5. 触发击杀、受伤、死亡和羁绊效果。
6. 检查双方是否仍有存活单位。

所有随机目标继续使用项目的种子随机数，保证同一 seed 与操作序列得到完全一致的结果。

### 5.4 新战斗事件

在现有 `ATTACK`、`DEATH`、`BATTLE_END` 基础上增加：

- `SKILL_CAST`
- `DAMAGE`
- `HEAL`
- `SHIELD_GAIN`
- `SHIELD_BREAK`
- `STATUS_APPLY`
- `STATUS_REMOVE`
- `MANA_CHANGE`
- `JUMP`
- `REVIVE`
- `TRAIT_TRIGGER`

界面只消费事件进行动画播放，不能反向修改战斗结果。

---

## 6. 数据与代码改造位置

| 模块 | 改造内容 |
|---|---|
| `config/cards.ts` | 扩展英雄数据；加入地区、职业、图片、法力、目标和技能配置 |
| `config/traits.ts` | 新增地区/职业羁绊配置与阈值 |
| `config/balance.ts` | 新增法力、伤害、护盾、控制等公共平衡参数 |
| `core/state.ts` | 扩展英雄实例、状态效果与战斗事件类型 |
| `core/bus.ts` | 补齐攻击、受伤、施法、击杀、死亡等触发器 |
| `core/effects.ts` | 实现伤害、治疗、护盾、控制、移动、复活等通用效果 |
| `core/targeting.ts` | 新增统一目标选择器 |
| `core/traits.ts` | 新增羁绊统计与应用逻辑 |
| `core/battle.ts` | 接入法力、施法、状态和新的结算顺序 |
| `bot/random.ts` | AI 增加羁绊、英雄价值和站位评分 |
| `web/ui.ts` | 新卡面、羁绊面板、技能说明、法力条和战斗事件播放 |
| `web/style.css` | LoL 风格但不依赖官方 Logo 的暗金/蓝色主题 |
| `web/public/assets/lol/` | Data Dragon 本地英雄头像、可选技能图标及来源清单 |
| `tests/` | 每类效果、英雄、羁绊和完整对局测试 |

角色专属技能尽量由“通用效果 + 参数”组合出来，避免每增加一名英雄就修改战斗主循环。

---

## 7. UI 方案

### 7.1 商店与卡牌

每张卡显示：

- 英雄头像与名称
- 品质边框
- 地区与职业图标/文字
- 攻击、生命、当前星级
- 技能图标和简短说明
- 购买价格

桌面端保持窄屏居中，移动端继续按竖屏设计。点击或长按卡牌显示完整技能、羁绊和升级后的数值。

### 7.2 棋盘

- 上方显示当前已激活羁绊。
- 前后排区分更明显。
- 拖动英雄时标出推荐位置。
- 刺客、射手等特殊目标规则在说明中明确展示。

### 7.3 战斗

- 英雄显示生命条、法力条与护盾条。
- 普攻、技能、治疗、控制使用不同颜色和浮字。
- 释放技能时突出英雄、技能名和目标。
- 保留原速、加速和跳过功能。

第一轮使用风格统一的占位头像即可，玩法稳定后再统一替换最终美术。

---

## 8. AI 与平衡

AI 由当前“买得起就买”升级为评分制：

```text
英雄评分 = 基础强度
         + 已有同名英雄的合成价值
         + 新增/升级羁绊价值
         + 当前阵容缺失职业价值
         + 稀有度与商店阶段修正
```

站位规则至少包含：

- 先锋优先前排。
- 射手、法师和辅助优先后排。
- 刺客根据敌方后排调整位置。
- 残血或关键输出避免放在最先受击位置。

平衡验证指标：

- 各英雄购买率、上阵率和胜率。
- 各地区/职业羁绊进入前四率。
- 2 星英雄形成回合分布。
- 单场战斗长度与单局总回合数。
- 各商店等级的金币利用率。

不要求第一轮达到竞技级平衡，但不能存在固定阵容明显碾压或战斗死循环。

---

## 9. 实施顺序

### 阶段 A：保护现有基线

- 确认并保留当前未提交修改。
- 记录当前 91 个测试和 Web 构建结果。
- 新机制分阶段接入，不一次性重写核心。

验收：原有测试仍通过，现有版本仍能完整跑局。

### 阶段 B：数据模型与静态卡池

- 扩展英雄配置结构。
- 加入 20 名英雄和英雄复制器。
- 新增地区、职业、技能描述和占位图片字段。
- UI 先正确显示新卡池。

验收：商店可购买、移动、出售和合成全部新英雄。

### 阶段 C：通用技能战斗引擎

- 实现法力、护盾、治疗、状态、目标选择和技能事件。
- 先用 4 名代表英雄覆盖主要机制。
- 补齐单元测试和确定性测试。

验收：代表英雄技能结果与事件回放一致。

### 阶段 D：20 名英雄技能

- 使用通用效果组合实现全部英雄。
- 为特殊技能增加最少量专用逻辑。
- 完成 1 星/2 星技能参数。

验收：20 名英雄均有可观察、可测试的技能差异。

### 阶段 E：羁绊与 AI

- 实现地区/职业统计与效果。
- AI 接入英雄、羁绊和站位评分。
- 批量运行完整对局并处理异常阵容。

验收：AI 会主动组成羁绊，1000 局无崩溃、无状态污染。

### 阶段 F：完整 Web 表现

- 更新卡面、羁绊面板、法力条和技能动画。
- 加入占位美术资源目录与统一资源映射。
- 完成移动端竖屏适配。

验收：玩家无需查看源码即可理解英雄技能、羁绊和战斗结果。

### 阶段 G：平衡与收尾

- 批量模拟统计英雄和羁绊表现。
- 调整数值、商店概率和伤害节奏。
- 更新玩法说明和当前数值文档。
- 运行测试、类型检查和生产构建。

验收：所有测试通过，Web 构建成功，可以从启动脚本完整游玩一局。

---

## 10. 最终验收标准

- 20 名英雄全部可购买、上阵、合成和施法。
- 每名英雄至少有一个与其他英雄有明显区别的技能。
- 5 个地区和 6 个职业羁绊能够正确激活、升级和取消。
- 战斗结果确定，可由同一 seed 重现。
- AI 能完成购买、组羁绊、站位和整局对战。
- Web 商店、棋盘、仓库和战斗回放在桌面及移动窄屏可用。
- 批量完整对局无崩溃、无无限战斗、无非法数值。
- 原有经济、合成、配对、淘汰逻辑没有回归。
- 测试、TypeScript 检查和 Vite 生产构建全部通过。

---

## 11. 已锁定默认决策

下一个 AI 不需要重新访谈，直接按以下默认值实施：

1. 使用本文列出的 20 名英雄，诺克萨斯蓝卡为德莱文，不使用锐雯。
2. 产品定位为本地个人、非商业试玩原型，不发布、不收费、不接广告。
3. 英雄头像优先从 Riot 官方 Data Dragon 获取，下载到本地后使用；下载失败时使用 CSS/文字占位，不阻塞玩法开发。
4. UI 使用原创的暗金/深蓝卡牌主题，不复制《英雄联盟》或《云顶之弈》的完整界面、Logo、字体或布局。
5. 英雄复制器使用原创 CSS 图形，不下载官方道具图标。
6. 未经用户另行确认，不把项目部署到公网，也不提交来源不明的图片、音频或字体。

---

## 12. 下一个 AI 的开工协议

### 12.1 工作区与已有修改

- 只操作实际仓库根目录 `D:\Codex_app`。若其他说明仍写着 `D:\Codex\_app`，以当前 Git 仓库根目录为准，不要复制出第二份项目。
- 首先运行 `git status --short` 和 `git diff -- core/economy.ts web/ui.ts`。
- 必须保留现有 `core/economy.ts` 与 `web/ui.ts` 修改，用小补丁合并。
- 不得运行 `git reset --hard`、`git checkout -- <file>` 或覆盖整个已有文件。
- 未经用户要求不要创建 commit，不要删除旧文档和启动脚本。

### 12.2 开工基线

在修改代码前依次运行：

```powershell
npm test
npx tsc --noEmit
npm run web:build
```

预期基线：12 个测试文件、91 个测试通过，类型检查和生产构建成功。若失败，先记录并诊断失败原因，不要通过删除测试或降低类型约束绕过。

### 12.3 实施纪律

- 每完成一个阶段先运行该阶段相关测试，再进入下一阶段。
- 战斗逻辑只能依赖配置和确定性 RNG；禁止使用 `Math.random()`、当前时间或网络响应参与战斗。
- Web 层只播放事件，不得反向修改核心状态。
- 真实棋盘上的卡牌不能因战斗掉血；战斗只修改快照。
- 新机制尽量用通用目标选择器和通用效果组合，英雄专用分支应保持最少。
- 不应为了兼容旧测试让旧卡重新进入正式商店。旧卡只进入 `LEGACY_CARDS` 和 `CARD_BY_ID`。

---

## 13. 官方英雄图片获取方案

### 13.1 来源与边界

只允许自动获取以下官方来源。Data Dragon 静态文件不需要 Riot API Key，不要申请或把任何账号密钥写进仓库：

- Data Dragon 说明：`https://developer.riotgames.com/docs/lol`
- 版本列表：`https://ddragon.leagueoflegends.com/api/versions.json`
- 中文英雄数据：`https://ddragon.leagueoflegends.com/cdn/{version}/data/zh_CN/champion.json`
- 方形头像：`https://ddragon.leagueoflegends.com/cdn/{version}/img/champion/{DataDragonId}.png`
- 默认原画：`https://ddragon.leagueoflegends.com/cdn/img/champion/splash/{DataDragonId}_0.jpg`
- 技能图标：先读取单英雄 JSON，再使用其中 `spells[].image.full` 拼接 `https://ddragon.leagueoflegends.com/cdn/{version}/img/spell/{file}`。

禁止从百度图片、Google 图片、Wiki、论坛、素材站、社交媒体或随机 CDN 抓图；禁止保存带水印、来源不明或由第三方再创作的立绘。浏览器搜索只用于找到官方文档，不能把搜索缩略图当素材。

Data Dragon 提供静态资产访问，但不代表此项目自动获得公开发行一款使用 Riot IP 的游戏的许可。Riot 当前开发者政策仍要求开发者遵守产品与 IP 限制。默认只用于本地、非商业原型；如果以后公开发布、商业化或提供下载，必须重新阅读以下页面并让用户决定是申请许可还是换成原创资产：

- `https://developer.riotgames.com/policies/general`
- `https://www.riotgames.com/en/legal`

### 13.2 固定版本与可复现下载

新增 `scripts/fetch-lol-assets.mjs`，只使用 Node 20+ 内置 `fetch`、`fs/promises` 和 `crypto`，不新增图片抓取依赖。脚本必须：

1. 读取 `web/public/assets/lol/manifest.json`。先提交一个 `dataDragonVersion: "16.18.1"`、`locale: "zh_CN"`、`assets: {}` 的初始清单；该版本于 2026-09-16 从官方版本列表核实。若清单已有版本，默认继续使用该版本。
2. 只有传入 `--latest` 时才读取 `versions.json` 的第一项并更新固定版本。
3. 请求 `zh_CN/champion.json`，验证 20 个 Data Dragon ID 全部存在。
4. 下载 20 张方形英雄头像到 `web/public/assets/lol/champions/`。当前 Vite 根目录是 `web`，放进 `web/public` 后才能稳定以 `/assets/...` URL 提供。
5. MVP 默认不下载大尺寸原画，避免无用二进制和仓库膨胀；只有实现确实使用背景原画时才通过 `--splashes` 下载。
6. 技能图标同理，只有 UI 已展示技能图标时才通过 `--spells` 下载。
7. 每个响应检查 HTTP 状态、`content-type`、非零长度，并设置合理超时。
8. 先写入同目录临时文件，校验成功后再原子改名，失败时不得留下半张图片。
9. 为每个文件记录 `sourceUrl`、相对路径、字节数和 SHA-256。
10. 重复运行时若哈希一致则不重写；网络失败时保留已有合法文件并返回非零退出码。

`package.json` 增加：

```json
{
  "scripts": {
    "assets:lol": "node scripts/fetch-lol-assets.mjs",
    "assets:lol:latest": "node scripts/fetch-lol-assets.mjs --latest"
  }
}
```

首次执行需要网络。如果执行环境限制网络，下一个 AI 应通过标准权限申请运行 `npm run assets:lol`，不要改用不受控的第三方下载站绕过限制。

### 13.3 ID 映射

代码中的 `id` 使用稳定的小写业务 ID，`dataDragonId` 使用官方大小写：

| 业务 ID | Data Dragon ID | 中文名 |
|---|---|---|
| `garen` | `Garen` | 盖伦 |
| `poppy` | `Poppy` | 波比 |
| `lux` | `Lux` | 拉克丝 |
| `jarvan_iv` | `JarvanIV` | 嘉文四世 |
| `darius` | `Darius` | 德莱厄斯 |
| `draven` | `Draven` | 德莱文 |
| `katarina` | `Katarina` | 卡特琳娜 |
| `swain` | `Swain` | 斯维因 |
| `shen` | `Shen` | 慎 |
| `yasuo` | `Yasuo` | 亚索 |
| `ahri` | `Ahri` | 阿狸 |
| `irelia` | `Irelia` | 艾瑞莉娅 |
| `vi` | `Vi` | 蔚 |
| `jinx` | `Jinx` | 金克丝 |
| `caitlyn` | `Caitlyn` | 凯特琳 |
| `ekko` | `Ekko` | 艾克 |
| `hecarim` | `Hecarim` | 赫卡里姆 |
| `kalista` | `Kalista` | 卡莉丝塔 |
| `thresh` | `Thresh` | 锤石 |
| `viego` | `Viego` | 佛耶戈 |

英雄卡的 `image` 指向本地路径，例如 `/assets/lol/champions/Garen.png`，运行时不直接热链官方 CDN。图片元素必须提供中文 `alt`，加载失败时显示英雄首字或地区渐变背景。

### 13.4 Git 与来源清单

默认只把下载的 Riot 二进制文件加入 `.gitignore`，不要忽略整个目录；这样清单和说明仍可跟踪。建议规则覆盖 `web/public/assets/lol/champions/*.png`、可选的 `splashes/*` 与 `spells/*`，但保留以下可审计文件：

- `scripts/fetch-lol-assets.mjs`
- `web/public/assets/lol/manifest.json`
- `web/public/assets/lol/README.md`

`manifest.json` 示例：

```json
{
  "dataDragonVersion": "16.18.1",
  "locale": "zh_CN",
  "assets": {
    "Garen": {
      "file": "champions/Garen.png",
      "sourceUrl": "https://ddragon.leagueoflegends.com/cdn/16.18.1/img/champion/Garen.png",
      "sha256": "下载后写入真实哈希",
      "bytes": 0
    }
  }
}
```

不要提交示例中的占位哈希或 `bytes: 0`；脚本必须生成真实数据。若用户明确要求把本地头像一并提交，再单独确认用途与资产政策。

### 13.5 页面声明

在 Web 页脚和项目说明中加入醒目的非官方声明，含义至少包括：本项目是本地非商业粉丝原型、未获 Riot Games 背书、相关名称/商标/资产归各自权利人所有。不要使用 Riot 或 LoL 官方 Logo 暗示合作关系。公开发布前必须按届时官方政策核对所需的准确声明与产品注册要求。

---

## 14. 代码数据模型定稿

### 14.1 卡牌配置

`config/cards.ts` 至少扩展为以下概念；具体 TypeScript 可使用判别联合，避免所有参数都塞进无类型的 `Record<string, number>`：

```ts
type Region = "demacia" | "noxus" | "ionia" | "piltover_zaun" | "shadow_isles";
type Profession = "vanguard" | "warrior" | "mage" | "marksman" | "assassin" | "support";

interface CardConfig {
  id: string;
  dataDragonId?: string;
  name: string;
  type: "hero" | "duplicator" | "role";
  quality: Quality;
  price: number;
  atk: number;
  hp: number;
  region: Region | null;
  professions: Profession[];
  image: string | null;
  maxMana: number;
  startMana: number;
  skill: ActiveSkillConfig | null;
  skills: SkillConfig[];
}
```

- `CARD_POOL`：只包含 20 名英雄和英雄复制器。
- `LEGACY_CARDS`：保留旧 `loey/lan/longye/fenliezhe` 等全部测试卡。
- `CARD_BY_ID`：由 `[...CARD_POOL, ...LEGACY_CARDS]` 构建，并校验 ID 唯一。
- 英雄复制器保持旧分裂者的合成补齐行为，但不参与任何羁绊、不能上场、没有法力或技能。
- `config/validate.ts` 新增：ID 唯一、20+1 数量、每地区恰好 4 名英雄、职业合法、职业数量足以激活阈值、图片路径格式、法力范围、技能效果参数、目标规则合法性。

### 14.2 战斗快照

持久 `CardInstance` 只保存局外会持续的数值；战斗临时属性全部进入 `BattleUnit`/`BattleUnitSnapshot`：

```ts
interface BattleUnitSnapshot {
  uid: number;
  owner: number;
  configId: string;
  level: 1 | 2;
  position: number;
  atk: number;
  hp: number;
  maxHp: number;
  mana: number;
  maxMana: number;
  shield: number;
  statuses: StatusInstance[];
  attackCount: number;
  castCount: number;
}
```

战斗结束后不得把 `hp/mana/shield/statuses` 写回玩家棋盘。星级成长建议保持可追溯：合成时基础攻血仍按现有规则求和，技能读取明确的 `[一星值, 二星值]`。

### 14.3 目标与效果判别联合

`core/targeting.ts` 统一实现：

- 敌方：`frontmost`、`backmost`、`lowest_hp`、`lowest_hp_backline`、`current_target`、`same_column`、`all_enemies`、`random_enemy`。
- 友方：`self`、`lowest_hp_ally`、`all_allies`、`random_ally`。
- 所有并列情况按位置、UID 的固定顺序破同分；只有明确写了 `random_*` 才消耗战斗 RNG。
- 后排为空时回退到前排，不能返回已死亡单位。

`core/effects.ts` 统一实现：

- `damage`、`heal`、`shield`、`stun`、`charm`、`attack_buff`、`damage_reduction`。
- `multi_hit`、`execute`、`break_shield`、`mana_gain`、`mana_drain`。
- 少量专用效果：`revive_rewind`、`steal_attack_on_kill`、`repeat_on_kill`。
- 伤害先扣护盾，再扣生命；治疗不超过 `maxHp`；死亡只触发一次；同一次效果链按配置顺序结算。

### 14.4 事件字段

每个事件都要能让 UI 在不读取可变核心状态的情况下播放：

```text
SKILL_CAST: caster, skillId, skillName, targets
DAMAGE: source, target, amount, absorbed, remainingHp
HEAL: source, target, amount, remainingHp
SHIELD_GAIN: source, target, amount, totalShield
SHIELD_BREAK: source, target, amount
STATUS_APPLY/REMOVE: source, target, status, duration
MANA_CHANGE: who, before, after, reason
TRAIT_TRIGGER: owner, trait, tier, targets
DEATH: who, killer
```

普通攻击可继续发 `ATTACK`，但实际扣血也必须经过统一伤害函数。事件顺序是确定性测试的一部分。

---

## 15. 20 名英雄的首轮可实现配置

下表是首轮功能规格，不要求复刻正式 LoL 数值。斜杠前后一星/二星，数值后续通过 1000 局模拟调整。

| 英雄 | 建议基础值（攻/血；法力） | 技能配置 |
|---|---|---|
| 盖伦 | 2/7；0/60 | 对全体敌人造成 3/5 伤害，自身获得 4/8 护盾。验证全体目标与自盾。 |
| 波比 | 2/8；20/70 | 对当前前排造成 6/10 伤害并眩晕 1/2 次行动，自身获得 3/6 护盾。 |
| 拉克丝 | 4/5；30/80 | 对敌方后排造成 8/14 伤害，为最低生命友方提供 5/9 护盾。验证后排回退。 |
| 嘉文四世 | 5/9；20/90 | 攻击敌方后排最高优先级目标 9/15，并为全体友方提供 2/4 护盾。 |
| 德莱厄斯 | 3/6；0/60 | 对最低生命敌人造成 8/13；目标低于 25%/35% 最大生命则斩杀，击杀后立即再行动一次，每次施法最多触发一次。 |
| 德莱文 | 4/5；20/70 | 向敌方后排连续投掷 2/3 次飞斧，每次造成攻击力 80%/90% 的伤害。 |
| 卡特琳娜 | 4/5；30/90 | 对敌方后排随机起点造成 3/5 段 2/3 伤害；使用战斗 RNG，但相同 seed 必须一致。 |
| 斯维因 | 5/10；40/100 | 对全体敌人造成 4/7 伤害，并按实际生命伤害的 35%/50% 治疗自己。 |
| 慎 | 2/8；25/70 | 为最低生命友方提供 7/12 护盾，并使其获得 1 次行动的 20%/30% 减伤。 |
| 亚索 | 4/6；0/70 | 连击当前目标 3 次，每次为攻击力 55%/70%；第三击眩晕 1 次行动。 |
| 阿狸 | 4/5；30/80 | 对敌方后排造成 9/15 伤害并魅惑 1 次行动；首轮可把魅惑实现为眩晕，但保留独立状态名和事件。 |
| 艾瑞莉娅 | 5/7；20/90 | 按当前生命从低到高选择至多 3/4 名敌人，各造成 5/8 伤害，不重复目标。 |
| 蔚 | 3/8；20/70 | 清除前排目标全部护盾，再造成 7/11 伤害并眩晕 1 次行动。 |
| 金克丝 | 4/5；20/80 | 主动攻击敌方后排 2/3 次；被动击杀后本场攻击力 +1/+2，立即获得 20 法力。 |
| 凯特琳 | 5/5；30/90 | 狙击后排最低生命敌人，造成 13/22 伤害；后排为空时选择全场最低生命敌人。 |
| 艾克 | 5/7；40/100 | 首次将受致命伤时取消该次死亡，生命恢复至最大生命 35%/55%，清除控制并获得 30/50 法力；每场一次。 |
| 赫卡里姆 | 3/9；20/80 | 对当前目标及其同列单位造成 6/10 伤害，自身按命中人数获得 1/2 护盾。 |
| 卡莉丝塔 | 4/5；10/60 | 对当前目标进行 4/6 段攻击，每段造成攻击力 45%/55%；验证多段伤害和中途死亡停止。 |
| 锤石 | 3/7；30/80 | 为最低生命友方提供 8/13 护盾，并对敌方后排造成 4/7 伤害与 1 次行动眩晕。 |
| 佛耶戈 | 6/7；20/90 | 攻击最低生命敌人造成 11/18；击杀后获得目标基础攻击力的 25%/40%，持续到本场战斗结束。 |

通用法力规则首轮固定为：普通攻击后 +10，受到至少 1 点实际生命伤害后 +5；护盾完全吸收的伤害不给受击法力；满法力时行动开始优先施法；施法后扣除 `maxMana` 而不是无条件归零，以便安全处理溢出法力。

先只接入盖伦、拉克丝、阿狸、卡莉丝塔并通过测试，再批量录入其余英雄。不要一次写 20 个英雄分支后再调试引擎。

---

## 16. 羁绊配置与确定性规则

新增 `config/traits.ts`，所有阈值和数值集中配置。每个英雄每种羁绊只计 1 次，英雄复制器不计数；同名英雄是否重复计数固定为“不重复”，以场上不同 `configId` 计数。

建议首轮数值：

| 羁绊 | 2 人 | 4 人 |
|---|---|---|
| 德玛西亚 | 全队开战 +3 护盾 | 改为 +6；首次破盾后攻击力 +2 |
| 诺克萨斯 | 每次击杀攻击力 +1 | 改为 +2，并治疗 10% 最大生命 |
| 艾欧尼亚 | 全队初始法力 +10 | 改为 +20；每次施法返还 10 法力 |
| 皮尔特沃夫/祖安 | 确定性强化 1 名友军：攻 +2、最大生命 +2 | 强化 2 名；技能效果倍率 +15% |
| 暗影岛 | 首名友军死亡后，存活友军攻击力 +1 | 每次友军死亡都触发，单个单位最多 3 层 |
| 先锋 | 最大生命 +15% | 最大生命 +30%，开战获得最大生命 10% 护盾 |
| 战士 | 攻击力 +15% | 攻击力 +30%，低于 40% 生命时减伤 20% |
| 法师 | 技能伤害/治疗/护盾 +15% | +30%，首次施法返还 15 法力 |
| 射手 | 每第 4 次普攻追加一次 60% 攻击 | 每第 3 次追加一次 80% 攻击 |
| 刺客 | 开战锁定后排，首次技能 +15% | 首次技能 +35%，后排为空才回退前排 |
| 辅助 | 治疗和护盾 +20% | +40%，单体友方增益复制给另一名最低生命友方 |

皮城祖安的“强化目标”不能用时间或 `Math.random()`。使用战斗 seed、玩家 ID、回合数派生 RNG，并在 `TRAIT_TRIGGER` 事件中记录选中的 UID。

所有百分比计算最终向下取整；原始效果为正但取整为 0 时至少结算 1 点。最大生命变化要同步调整当前生命上限，但不能把受伤单位按比例回满。

---

## 17. AI 改造规格

AI 保持独立 RNG，并把单步合法动作接口保留。购买评分建议拆为可测试纯函数：

```text
总分 = 基础卡强度
     + 同名数量带来的合成分
     + 激活新羁绊的分
     + 2→4 羁绊升级分
     + 补齐前排/输出/辅助的阵容分
     + 当前商店等级与费用修正
     - 手牌拥堵惩罚
```

最低行为要求：

- 能比较商店三个槽位，不再只买第一个买得起的英雄。
- 优先完成三合一，但不能为了低价值合成永远停止升级。
- 阵容未满时优先覆盖前排与后排；阵容已满时能出售或替换低分一星英雄。
- 先锋、战士默认位置 1～3；射手、法师、辅助默认位置 4～6；刺客按敌方后排位置选择列。
- 站位函数应是确定性的；同分时按 UID/位置破同分。
- 新增调试统计：英雄购买次数、上场次数、胜率、羁绊激活次数、平均回合数。统计只用于测试/CLI，不进入核心状态。

---

## 18. Web UI 明细

保持现有交互不退化，并按以下顺序增量修改：

1. 卡面加入本地头像、地区、职业、技能名和法力值；窄屏时技能说明可折叠。
2. 棋盘上方加入羁绊面板，显示 `当前人数/下一阈值`，已激活层级高亮。
3. 战斗单位显示生命、护盾、法力三层信息；护盾不能伪装成额外生命。
4. `SKILL_CAST` 显示技能名；伤害、治疗、护盾、控制使用不同颜色和短浮字。
5. 继续保留拖动购买、上阵交换、出售、准备、加速与跳过。
6. 动画层只读事件；按“跳过”必须立即播放到最终快照并正确清理定时器。
7. 图片加载失败时显示确定性的 CSS 占位，不显示破图图标。
8. 不使用官方 Logo、游戏客户端截图、官方字体或来源不明音效。

---

## 19. 测试矩阵与阶段门

建议新增测试文件：

- `tests/targeting.test.ts`：前排、后排、最低生命、同列、空后排回退、同分规则。
- `tests/effects.test.ts`：伤害、护盾吸收、破盾、治疗上限、控制、多段攻击、中途死亡。
- `tests/mana.test.ts`：攻击回蓝、受伤回蓝、满蓝施法、溢出扣蓝、眩晕跳过。
- `tests/traits.test.ts`：2/4 阈值、去重、复制器排除、所有 11 个羁绊的核心效果。
- `tests/champions.test.ts`：20 名英雄至少各 1 个行为断言，代表英雄覆盖完整事件序列。
- `tests/assets.test.ts`：20 个图片路径唯一、映射完整；本地图片存在时校验非空，不强制 CI 联网。
- `tests/ai.test.ts`：合成、羁绊升级、前后排站位、同 seed 决策一致。
- 扩展 `tests/determinism.test.ts`：技能、随机目标、皮城强化、事件日志完全一致。
- 扩展 `tests/fullgame.test.ts`：新卡池 1000 局无崩溃、无 NaN、无负护盾、无无限战斗。

每阶段门：

| 阶段 | 必须通过后才能继续 |
|---|---|
| A 基线 | 原 91 测试、类型检查、构建 |
| B 卡池 | 配置校验、购买/出售/移动/合成、新图片映射 |
| C 引擎 | 目标、法力、效果、4 名代表英雄、确定性 |
| D 全英雄 | 20 名英雄行为测试、事件回放 |
| E 羁绊/AI | 羁绊阈值、AI 站位、100 局快速模拟 |
| F UI | 构建、桌面和窄屏人工检查、缺图回退 |
| G 收尾 | 全测试、类型检查、1000 局、生产构建 |

最终命令：

```powershell
npm test
npx tsc --noEmit
npm run sim -- --batch 1000
npm run web:build
```

最终报告必须列出：修改文件、测试数、1000 局耗时与平均回合数、构建结果、仍未完成事项、资产版本与来源、Git 工作区状态。不能只说“已完成”。

---

## 20. 推荐的逐步提交/交接顺序

即使不创建 Git commit，也按以下逻辑批次工作，便于中途交接：

1. **数据批次**：类型、20+1 卡池、Legacy 兼容、配置校验。
2. **资产批次**：下载脚本、清单、20 张头像、缺图回退、非官方声明。
3. **引擎批次**：目标选择、效果、法力、事件、快照隔离。
4. **代表英雄批次**：盖伦、拉克丝、阿狸、卡莉丝塔及测试。
5. **全英雄批次**：其余 16 名英雄与行为测试。
6. **羁绊批次**：地区、职业、配置与测试。
7. **AI 批次**：购买评分、站位、统计、确定性。
8. **UI 批次**：卡面、羁绊、三条状态、事件动画、响应式布局。
9. **验收批次**：回归、类型检查、1000 局、生产构建、文档。

若某批次未完成，更新本文顶部状态，并写清最后通过的命令与当前失败项，让后续 AI 从该批次继续，不要重做已经验证的部分。
