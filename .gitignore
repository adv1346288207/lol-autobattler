node_modules/
dist/
*.log
.DS_Store

# Data Dragon 资源：champions / portraits / items 已入库（共 55 个文件约 1.6 MB），
# 这样 clone 下来不用联网就能构建，部署也不依赖 Data Dragon 是否可用。
# 体积更大的 splashes / spells 目前没用到，先不入库；要加的话删掉下面两行并跑 npm run assets:lol。
web/public/assets/lol/splashes/
web/public/assets/lol/spells/

# 自走自检截图输出
shots/
