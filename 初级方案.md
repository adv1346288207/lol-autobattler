# 悠悠牌同款 · 初级方案（MVP 框架设计）

> 版本：v0.4 ｜ 日期：2026-08-15（v0.3 → v0.4：橙卡概率确认 L1-3=0/L4=10%/L5=30%；术语区分橙卡=品质、金卡=合成产物；卡牌攻血转占位待实机截图）
> 定位：搭建一个**能完整跑通一局**的最简框架（8 人淘汰赛制 + 商店经济 + 自动战斗），后续功能全部在它上面叠加
> 原则：本期只定方案不写代码；所有数值为 v0 拍板值，**集中在一张配置表里**，随时可调
> 依据：以用户实测为准（已勘误：卡牌最高 2 级即金卡，不可再合成 3 级）

---

## 1. 范围

### 1.1 本期做（IN）

| # | 模块 | 说明 |
|---|---|---|
| 1 | 8 人回合制对战框架 | 玩家 + 7 个呆 AI，车轮战淘汰，名次结算（保留 8 人结构是为了联机时直接把 AI 换成远程玩家） |
| 2 | 回合循环状态机 | 商店回合 → 战斗回合 → 伤害结算 → 下回合，直至只剩 1 人 |
| 3 | 商店系统 | 每回合自动刷新 3 张、手动刷新（1 金）、出售（=买入价）、等级 1~5、等级概率表 |
| 4 | 经验/升级系统 | 每回合自动 +1 经验，升级消耗经验，可 2 金买 1 经验 |
| 5 | 金币系统 | 阶梯递增发放（2 起封顶 20），买牌/刷新/买经验收支 |
| 6 | 三合一合成 | 3 张同卡 → 金卡（最高 2 级，攻/血=三张之和）；位置 1→6 扫描；**分裂者**万能补齐；**只在购买阶段合并** |
| 7 | 自动战斗 | 6 卡位（前排 3 后排 3）、从左到右从前到后交替攻击、打前排 |
| 8 | 伤害扣血与淘汰 | 饱和曲线公式（按胜方存活卡剩余血量总和，封顶 30）；平局双方各扣 3；血量归零出局 |
| 9 | 初始卡池 | 10 张角色卡 + 1 张万能牌分裂者（含兰、龙野等带商店阶段被动的卡） |
| 10 | CLI 文本渲染 | 每回合打印棋盘/商店/血量，可交互试玩 |
| 11 | 测试体系 | 单测 + 确定性 + 不变量 + 千局模拟（见第 10 章） |

### 1.2 本期不做（OUT，但预留扩展点）

| 不做项 | 预留方式 |
|---|---|
| 联机 | Action 驱动架构（见 2.2），将来 Action 上移服务端即可 |
| 地图/地图Buff | 对局配置里保留 `map: null` 字段 |
| 武器/道具/投掷物/消耗卡/装备槽 | 卡牌表已有 `type` 字段；目标选择独立成函数，将来按武器类型扩展；**KAC-变色龙万能武器规则已定义（见 6.8），等武器系统上线即启用** |
| 战斗词条（快手/遗言/成长/消灭/护盾等） | 事件总线 + 效果注册表骨架本期就建好，只实现兰/龙野两个商店被动 |
| 羁绊 | 卡牌表已有 `faction` 字段，统计器后置 |
| 法宝/战术升级/Buff 三选一 | 回合钩子处预留插入点 |
| PVE 怪物回合 | 配对阶段预留插入点 |
| 段位/通行证/钻石赛/美术UI | 完全后置 |

---

## 2. 三条地基原则（最重要的部分）

### 2.1 确定性（Determinism）

- 核心逻辑（战斗、经济、合成、配对）全部为**纯函数**：`(state, action, rng) → (newState, events)`
- 唯一随机源：自定义种子随机数生成器（mulberry32 或 xorshift），全对局只用一个 RNG 实例
- 禁止：`Math.random()`、`Date.now()`、遍历对象键、浮点比较
- 收益：同一 seed + 同一操作序列 → 完全相同的对局结果 → **回放、调试、未来联机校验**全靠它

### 2.2 动作驱动（Action-driven）

玩家/AI 的一切操作 = 一条 Action，模拟器只消费 Action 推进状态：

```
buy(cardId) | sell(slotId) | refresh() | upgradeShop() | move(cardId, pos) | endShopPhase()
```

- 非法 Action（钱不够、位已满等）直接拒绝并报错，**状态永不被非法操作污染**
- AI 与人类玩家走完全相同的 Action 通道 → 联机时"远程玩家的 Action"和"本地 AI 的 Action"无差别

### 2.3 全配置化（Data-driven）

- 卡牌数值、品质、技能、价格、商店概率、经济参数、伤害公式，**全部进配置表**（`config/` 目录）
- 代码里零硬编码数值；平衡调整只改表不动代码
- 技能 = 数据：`{ trigger: "turn_start", effect: "gain_exp", params: {n: 1} }`，新卡 = 新配置行

---

## 3. 整体架构

```
┌──────────────────────────────────────────────────┐
│  渲染层 renderer/                                  │
│  CLI 文本渲染（本期）/ 图形 UI（将来替换）            │
│  只读 GameState + 战斗事件日志，不做任何逻辑          │
└──────────────────────┬───────────────────────────┘
                       │ 订阅
┌──────────────────────▼───────────────────────────┐
│  控制器 controller/                                │
│  收集 Action（CLI 输入 / AI / 将来网络包）           │
│  提交给模拟器，非法操作在此被拒                      │
└──────────────────────┬───────────────────────────┘
                       │ action
┌──────────────────────▼───────────────────────────┐
│  核心模拟器 core/（纯逻辑，零 UI 依赖，确定性）        │
│                                                    │
│  ├─ 状态机 phaseMachine：回合阶段流转               │
│  ├─ 商店 shop：刷牌/概率/购买/出售/刷新             │
│  ├─ 经济 economy：金币收支/经验/升级                │
│  ├─ 合成 combine：3=1 扫描合并（含万能牌）           │
│  ├─ 战斗 battle：行动顺序/目标选择/伤害             │
│  ├─ 伤害结算 damage：存活HP和→饱和曲线→扣血/平局    │
│  ├─ 配对 matchmaking：8 人随机配对                  │
│  ├─ 事件总线 bus：trigger 注册/分发（词条扩展点）     │
│  └─ 效果注册表 effects：effect 实现（词条扩展点）     │
└──────────────────────┬───────────────────────────┘
                       │ 读取
┌──────────────────────▼───────────────────────────┐
│  配置层 config/（数据表，全部可热调）                │
│  cards / economy / shop / damage / game           │
└──────────────────────────────────────────────────┘
```

**关键设计：战斗事件日志（Event Log）**

战斗阶段模拟器不直接"改血"，而是顺序产出事件流：

```
BATTLE_START → ATTACK{from,to,dmg} → DEATH{who} → ... → BATTLE_END{winner, survivors}
```

渲染层消费事件流播放表现；回放 = 重跑事件流。这条管道将来联机同步、观战、录像全都要用。

---

## 4. 对局流程（状态机）

```
一局 = 8 人开局
  │
  ├─[每回合循环]
  │   ├─ 回合开始：发金币（阶梯制）、自动+1经验、兰/龙野类被动触发、商店自动刷新 3 张
  │   ├─ 商店阶段：玩家与 7 个 AI 各自决策（买/卖/刷新/升级/买经验/调站位）
  │   │            每次获得卡牌后立即检测三合一（只在购买阶段合并）
  │   ├─ 配对阶段：存活者随机两两配对（见 6.5）
  │   ├─ 战斗阶段：每对玩家依次自动战斗，产出事件日志（战斗中获得卡不合并，下回合购买阶段统一处理）
  │   ├─ 伤害阶段：败者扣血（饱和曲线封顶30），平局双方各扣 3；血量≤0 出局（记录名次）
  │   └─ 检查：存活=1 → 结束并结算名次；回合数≥60 → 强制按血量排名结束
  │
  └─ 终局：名次表输出
```

- 商店阶段：并行决策、串行结算（玩家先、AI 后），Action 顺序不影响确定性（AI 顺序固定）
- 战斗阶段：玩家不可操作，模拟器按配对顺序逐场结算

---

## 5. 数据模型（schema 草图）

```ts
// ── 卡牌（配置）─────────────────────────────
interface CardConfig {
  id: string;            // "lan" 唯一标识
  name: string;          // "兰"
  type: "role";          // 本期只有 role；将来扩展 weapon/item/throwable/consumable
  quality: "green"|"blue"|"purple"|"orange"|"gold";  // gold 仅万能牌分裂者使用（合成产出的金卡不走 quality 字段，见 CardInstance.level）
  atk: number;           // 基础攻击
  hp: number;            // 基础血量
  price: number;         // 商店售价
  faction: string | null;   // 阵营（本期不启用，字段先留）
  wildcard?: "role"|"weapon";  // 万能补齐牌标记（分裂者=role，变色龙=weapon）
  skills: SkillConfig[];    // 技能（本期只实现商店阶段被动）
}

// 技能 = 数据（扩展点核心）
interface SkillConfig {
  trigger: "turn_start" | "on_attack" | "on_death" | "on_kill" | "on_battle_start" | "on_hurt" | "on_shop_refresh";
  effect: "gain_exp" | "free_refresh" | "gain_gold" | /* 将来: "extra_attack" "summon" ... */ string;
  params: Record<string, number>;
  once_per_turn?: boolean;
}

// ── 卡牌实例（对局中）───────────────────────
interface CardInstance {
  uid: number;           // 实例唯一 ID
  configId: string;
  level: 1 | 2;          // 1=普通 2=金卡（最高级，不可再合成）
  atk: number; hp: number;   // 当前值（含一切加成后的值，只存当前态）
  position: number | null;   // 场上位置 1~6（前3后3），null=在手牌
  isFreeRefreshUsed?: boolean; // 龙野类每回合状态位
}

// ── 玩家状态 ────────────────────────────────
interface PlayerState {
  id: number;
  isHuman: boolean;
  hp: number;            // 初始 30
  gold: number;
  exp: number;           // 经验（升级消耗）
  shopLevel: number;     // 1~5
  hand: CardInstance[];  // 手牌（上限 15）
  board: (CardInstance|null)[];  // 长度 6
  shop: string[];        // 当前商店 3 个 configId
  freeRefresh: number;   // 免费刷新次数（龙野给 1，金龙野给 2）
  eliminated: boolean;   // 已出局
  rank: number | null;   // 名次
}

// ── 全局状态 ────────────────────────────────
interface GameState {
  round: number;         // 当前回合数
  phase: "shop" | "pair" | "battle" | "damage" | "ended";
  players: PlayerState[];
  seed: number;
  rngState: number;      // RNG 内部状态（随状态一起快照/回放）
  battleLog: BattleEvent[];
  version: string;       // 规则版本号（将来改动规则时兼容旧回放）
}

// ── 操作（Action）───────────────────────────
type Action =
  | { type:"buy"; player:number; shopIndex:0|1|2 }
  | { type:"sell"; player:number; cardUid:number }
  | { type:"refresh"; player:number }
  | { type:"upgradeShop"; player:number }
  | { type:"buyExp"; player:number }
  | { type:"move"; player:number; cardUid:number; position:number }
  | { type:"endShop"; player:number };

// ── 战斗事件（Event）────────────────────────
type BattleEvent =
  | { type:"BATTLE_START"; a:number; b:number }
  | { type:"ATTACK"; from:number; to:number; dmg:number }
  | { type:"DEATH"; who:number }
  | { type:"COMBINE"; cardUid:number; configId:string }   // 三合一（购买阶段事件）
  | { type:"BATTLE_END"; winner:number|null; survivors:CardSummary[] };  // winner=null 为平局
```

**卡牌状态只存当前态**（atk/hp 直接存数值），不存"加成来源链"——这是防复杂度爆炸的关键：本期没有加成系统，将来加词条时加成在触发瞬间直接落到数值上，天然兼容。

---

## 6. 数值方案 v0（全部集中在 config/ 一张表）

### 6.1 对局参数

| 参数 | v0 值 | 说明 |
|---|---|---|
| 人数 | 8（1 玩家 + 7 AI） | 结构不变，联机后换人 |
| 玩家初始血量 | **30** | 用户实测确认 |
| 单局最大扣血 | **30** | 伤害封顶（即后期可一击带走满血） |
| 平局伤害 | 双方各扣 **3** | 固定值（为将来遗言等词条预留；MVP 无词条时交替攻击理论上不会平局） |
| 回合上限 | 60 | 到 60 回合未分胜负 → 按血量从高到低排名 |
| 单场战斗步数上限 | **3000** | 用户确认（2026-08-17）：我一回合对手一回合，每步一次行动 |

### 6.2 金币（阶梯递增）

| 回合 | 发放 |
|---|---|
| 第 1 回合 | **2** |
| 第 2~10 回合 | 每回合 **+1**（R10 = 11） |
| 第 11 回合起 | 每回合 **+2**，封顶 **20**（R15 起恒为 20） |

- 无利息、无连胜/连败奖励（接口留好）

### 6.3 经验与商店升级（机制 2026-08-17 修正）

| 参数 | v0 值 |
|---|---|
| 每回合自动获得 | **+1 经验**（所有玩家） |
| 兰 | 每回合额外 +1 经验（商店被动，见卡池表） |
| 金兰 | 每回合额外 +2 经验 |
| 升级所需经验 | 1→2: **2** ｜ 2→3: **8**（用户举例）｜ 3→4: 12 占位 ｜ 4→5: 16 占位 |
| **升级付款** | **差多少经验 → 一次性付多少金币（1经验=1金币）**；经验≥所需 → 免费；多余经验保留到下个等级。**无"买经验"操作** |

### 6.4 商店

| 参数 | v0 值 |
|---|---|
| 每回合自动刷新 | 3 张（回合开始时自动换一批，免费） |
| 手动刷新 | **1 金/次**（消耗免费刷新次数优先） |
| 卡牌售价 | 绿 **1** ｜ 蓝 **2** ｜ 紫 **3** ｜ 橙 **4** ｜ 金（分裂者）**4** |
| 出售价 | **= 买入价** |
| 手牌上限 | **15**（超出时禁止购买，需先卖或上阵） |

**商店概率表（按商店等级，单位 %）**：

| 商店等级 | 绿 | 蓝 | 紫 | 橙 | 金（=分裂者） |
|---|---|---|---|---|---|
| 1 | 占位 | 占位 | 占位 | **0（已确认）** | 3（提案） |
| 2 | 占位 | 占位 | 占位 | **0（已确认）** | 3（提案） |
| 3 | 占位 | 占位 | 占位 | **0（已确认）** | 3（提案） |
| 4 | 占位 | 占位 | 占位 | **10（已确认）** | 3（提案） |
| 5 | 占位 | 占位 | 占位 | **30（已确认）** | 10（提案） |

- **术语约定**：橙卡 = 品质档位；金卡 = 三张同卡合成的产物（独立于品质体系）
- 橙卡概率已由用户确认：L1~L3 = 0%、L4 = 10%、L5 = 30%；绿/蓝/紫各等级完整分布**待用户提供**（先用占位值跑通框架，拿到数据后只改配置表）
- MVP 中金卡列只产出**分裂者**（合成产出的金卡不进商店）；将来 5 级商店直刷金卡时共用此列
- 概率表作为独立配置，将来每局卡池限制（按阵营过滤）直接在"抽卡前过滤候选池"一处实现

### 6.5 配对规则

- 每回合将存活玩家随机两两配对；人数为奇数时随机 1 人**轮空**（本回合不扣血）
- 约束：尽量不与上一回合的对手重复（有可选组合时避开）
- 全部通过 seed RNG 决策 → 可回放

### 6.6 伤害公式（饱和曲线）

```
伤害 = min( 30, round( 30 × ( 1 − e^( −Σ存活HP ÷ K ) ) ) )

Σ存活HP = 胜方场上所有存活卡当前剩余血量之和
K = 40（MVP 量级下的饱和度系数，配置可调；将来数值膨胀到几百上千血时调大 K 即可）
```

| Σ存活HP | 伤害 |
|---|---|
| 10 | 7 |
| 30 | 16 |
| 50 | 21 |
| 80 | 26 |
| 120 | 29 |
| 200+ | 30 封顶 |

- 特征：血量越高伤害越高、增长递减、封顶 30——与用户直觉一致（原版量级约 300血→20伤害、1000血→28伤害，K 调大后同曲线吻合）
- 平局：双方各扣 **3**（固定值）

### 6.7 三合一合成（购买阶段专属）

**核心规则**：只要有 3 张同卡（位置不限：场上任意位置 + 手牌任意组合），**在购买阶段**立即合并为金卡；战斗过程中获得的卡**不会**在战斗过程中合并，统一到下一回合购买阶段处理。

**金卡属性**：攻击 = 三张之和，血量 = 三张之和（含万能牌自身数值）。

**扫描与落点规则**（场上位置 1→6 依次扫描）：

| # | 情形 | 结果 |
|---|---|---|
| 1 | 1 号位兰 + 仓库兰 + 分裂者 | → 1 号位变金兰（1 号位优先于 2 号位龙野的竞争） |
| 2 | 1/3/6 号位各持 1 张兰（3 张真卡全在场上） | → 自动合并，金兰留在 1 号位（第一个被扫描位置），3/6 号位空出 |
| 3 | 仓库 3 张真卡（场上无锚点） | → 直接合成，金卡进手牌 |
| 4 | 2 个位置各持 1 张相同武器 + 变色龙 | → **不合并**（两个角色持有着；武器规则，变色龙上线后启用） |
| 5 | 1 个位置持有武器 + 仓库同款武器 + 变色龙 | → 合并为金武器（变色龙规则，武器系统上线后启用） |
| 6 | 1 号位兰 + 2 号位龙野 + 仓库兰/龙野 + 分裂者 | → 先与兰合并（1 号位优先），分裂者被消耗，龙野不再合 |

**其他规则**：
- 金卡为最高 2 级，**不可再合成**（用户勘误）
- 万能牌每次合并消耗 1 张；3 张分裂者不会合成（金卡封顶）
- 合并检测时机：购买阶段内每次"获得卡牌"（购买/万能补齐等）后立即执行；所有操作都在购买阶段进行

### 6.8 万能牌（分裂者 / KAC-变色龙）

| 万能牌 | 类型 | 补齐范围 | 属性 | MVP 状态 |
|---|---|---|---|---|
| 分裂者 | role | 角色卡三合一 | 金卡品质、售价 4 金、有攻血（**数值待定**）、可上阵 | **本期实现** |
| KAC-变色龙 | weapon | 武器卡三合一 | 待武器系统上线时定 | 规则已定义，等武器系统上线即启用 |

- 分裂者走商店**金卡概率列**，全等级可刷（见 6.4 概率表）
- 万能牌参与合并时充当 1 张"同卡"，产出金卡归属被补齐的那张卡（如 分裂者+2兰 → 金兰），合并时分裂者的攻血计入金卡之和
- 万能牌自身不参与普通三合一（3 张分裂者不会合成金分裂者）

---

## 7. 战斗规则 v0

- 场地：双方各 6 卡位，位置编号 1~6（1~3 前排，4~6 后排；1 号位 = 最前）
- **行动顺序**：交替进行——我方 1 号位行动 → 敌方 1 号位行动 → 我方 2 号位 → 敌方 2 号位 → …（已死亡者跳过）
- **攻击目标**（本期无武器系统）：对方**最前排存活单位** = 前排（1~3）中编号最小者；前排全灭后打后排编号最小者
- 伤害 = 攻击者攻击力，直接扣目标血量；无反击/护盾/词条（词条走事件总线，接口本期建好）
- 一方全部阵亡 → 判负；同回合双方全灭 → 平局（双方各扣 3）
- 战斗阶段产出事件日志，渲染层只播事件不改状态

---

## 8. 初始卡池 v0

### 8.1 角色卡（10 张）

> ⚠️ 以下攻/血均为**占位数值**（仅用于跑通框架），待用户实机截图提供全部角色初始属性后统一替换配置表。

| id | 名称 | 品质 | 攻 | 血 | 价 | 技能（商店阶段被动） |
|---|---|---|---|---|---|---|
| loey | 洛伊 | 绿 | 1 | 5 | 1 | 无（白板肉盾，验证站位用） |
| seth | 赛斯 | 绿 | 2 | 3 | 1 | 无 |
| swat | 斯沃特 | 绿 | 2 | 4 | 1 | 无 |
| lan | 兰 | 蓝 | 2 | 2 | 2 | **每回合额外 +1 经验（金卡 +2）** |
| longye | 龙野 | 蓝 | 2 | 3 | 2 | **每回合获得 1 次免费刷新，不累积（金卡 2 次）** |
| aogu | 傲骨 | 蓝 | 3 | 3 | 2 | 无 |
| daofeng | 刀锋 | 紫 | 4 | 6 | 3 | 无（将来挂"成长：友方+1+1"） |
| zhujiuyin | 烛九阴 | 紫 | 4 | 8 | 3 | 无（将来挂成长词条） |
| taitan | 泰坦 | 橙 | 6 | 12 | 4 | 无 |
| wumei | 妩媚妖姬 | 橙 | 5 | 8 | 4 | 无 |

- 金卡 = 攻/血均为三张之和（如金泰坦 = 18 攻 / 36 血；金洛伊 = 3 攻 / 15 血）

### 8.2 万能牌（1 张）

| id | 名称 | 品质 | 攻 | 血 | 价 | 说明 |
|---|---|---|---|---|---|---|
| fenliezhe | 分裂者 | 金 | **待定** | **待定** | 4 | 角色三合一万能补齐，合并时消耗，自身攻血计入金卡之和 |

---

## 9. AI 设计（呆 AI）

```ts
interface Bot {
  decide(state: PublicStateOf(playerId)): Action[];
}
```

- **接口与人类玩家完全一致**（Action 队列），将来替换为网络玩家零改动
- v0 策略（随机 + 少量预算规则，行为可预测便于测试）：
  1. 金 ≥ 5：买满 3 张（优先买"已有 2 张的同卡"凑三合一）
  2. 金 ≥ 3：买 1~2 张
  3. 每回合 10% 概率手动刷新（有免费刷新则必用）
  4. 经验够升级 → 必升级
  5. 买到的卡按位置 1~6 顺序填坑上阵
  6. 万能牌（分裂者）按普通卡处理照买（三合一自动触发，AI 无需额外逻辑）
- 留出策略分档：`RandomBot`（本期）→ 将来 `GreedyBot`（对比测试用）

---

## 10. 测试方案（与开发同步做，不是收尾再做）

### T1 单元测试（每模块）

| 模块 | 测什么 |
|---|---|
| economy | 金币阶梯发放（R1=2、R10=11、R15 起=20 封顶）；收支恒等式；买经验扣费；负数操作被拒 |
| shop | 概率表取样分布（χ² 校验）；刷新扣 1 金与免费刷新优先级；手牌上限 15 拦截；金卡列（分裂者）全等级可刷 |
| combine | 2 张不合成、3 张必合成、金卡不可再合成；**金卡攻血=三张之和；扫描优先级（1 号位优先）；真卡三张全场上合并落点=第一位置；仓库 3 张直接合进手牌；双持有不合并；万能补齐规则；分裂者消耗且自身数值计入；只在购买阶段合并（战斗中获得不即时合）** |
| battle | 行动顺序精确断言（1对1、2对3、空位跳过）；目标选择；平局分支（构造双方同时全灭的边界） |
| damage | 饱和曲线各锚点（10→7、30→16、50→21、80→26、120→29、200+→30）；平局扣 3；血量归零出局 |
| matchmaking | 奇数轮空；不重复对手约束；8→7→6…→1 人数收敛 |

### T2 确定性测试

- 同一 `seed` + 同一 Action 序列 → `GameState` 序列化 hash 完全一致
- 战斗事件日志 hash 完全一致（含合并事件顺序）

### T3 不变量测试（随机 fuzz）

- 随机生成 10 万条合法 Action 序列灌入模拟器，全程断言：
  - `hp ∈ [0,30]`、`gold ≥ 0`、`exp ≥ 0`
  - 上阵数 ≤ 6、手牌数 ≤ 15、`shopLevel ∈ [1,5]`
  - **卡牌总数守恒**（所有卡实例 = 出场 + 手牌 + 商店 + 已售回收，账目平衡）
  - 非法 Action 被拒后状态与拒绝前完全一致（快照对比）
  - 60 回合内必然终止

### T4 全量模拟对局（集成测试）

- 7 AI 跑 **1000 局**，断言：
  - 0 崩溃、0 未捕获异常
  - 100% 正常终止（无死循环、无不淘汰）
  - 名次分布均匀性（各 AI 无明显固定胜者 = 随机性正常）
  - 输出统计：平均局时长/平均回合数/平均伤害曲线（供平衡调参）

### T5 回放测试

- 记录 `seed + ActionLog`，重放至相同终局（名次表一致）
- 这是将来联机断线重连、对局录像的地基

### T6 CLI 人工试玩

- 交互式命令：`buy 1` / `sell 3` / `refresh` / `up` / `buyexp` / `place <uid> <pos>` / `end`
- 每回合打印：血量榜、商店（含概率）、手牌、棋盘（前后排可视化）、三合一触发日志、战斗过程事件流
- 用于人肉验证手感与数值节奏

### 验收标准（M4 里程碑门槛）

- T1~T3 全绿、T4 千局 0 异常、T5 回放一致、T6 可完整手动玩一局
- 单局模拟性能：< 100ms（将来千局回归/服务端负载的基础）

---

## 11. 工程结构与技术选型

### 11.1 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 语言 | **TypeScript**（strict 模式） | 类型即文档（schema 落地）；核心逻辑将来可无缝复用到 Node 服务端 |
| 运行时 | Node ≥ 20 | CLI 跑模拟/测试 |
| 测试框架 | **vitest** | 快、TS 原生、watch 模式适合边开发边测 |
| 随机源 | 自实现 mulberry32（seedable） | 确定性前提 |
| 核心依赖 | **零依赖**（仅测试框架） | 模拟器保持纯净，将来嵌入任何引擎 |
| 配置 | TS 常量表（`config/*.ts`），可选 zod 校验 | 类型安全 + 启动时校验配置合法性 |

### 11.2 目录结构

```
uucard_game/
├─ 初级方案.md            # 本文
├─ 悠悠牌全局文档.md       # 调研文档
├─ core/                  # 核心模拟器（纯逻辑，零依赖，确定性）
│  ├─ rng.ts              # seedable RNG
│  ├─ state.ts            # GameState/PlayerState 类型与工厂
│  ├─ actions.ts          # Action 定义与合法性校验
│  ├─ phase.ts            # 回合状态机
│  ├─ shop.ts             # 商店刷牌/购买/出售/刷新
│  ├─ economy.ts          # 金币/经验/升级
│  ├─ combine.ts          # 三合一扫描合并（含万能牌）
│  ├─ battle.ts           # 战斗模拟（行动顺序/目标选择/伤害）
│  ├─ damage.ts           # 伤害公式/平局/淘汰
│  ├─ matchmaking.ts      # 配对
│  ├─ bus.ts              # 事件总线（词条扩展点）
│  └─ effects.ts          # 效果注册表（词条扩展点）
├─ config/                # 全配置表
│  ├─ cards.ts            # 卡池
│  ├─ economy.ts          # 金币/经验/升级参数
│  ├─ shop.ts             # 概率表/刷新价/卡价/上限
│  ├─ damage.ts           # 血量/伤害公式参数（K=40）
│  └─ game.ts             # 人数/回合上限等
├─ bot/                   # AI（RandomBot 起步）
├─ cli/                   # 文本渲染器 + 交互入口
├─ tests/                 # T1~T5 全部测试
└─ package.json
```

---

## 12. 里程碑（每步都有可验收产出）

| 里程碑 | 内容 | 验收 |
|---|---|---|
| **M0 脚手架**（0.5 天） | 目录/TS/vitest 跑通；RNG + GameState 工厂 + 阶段机骨架；配置表加载与校验 | T1 首个测试绿 |
| **M1 商店经济**（1 天） | 金币阶梯/经验/升级/商店/刷新/购买/出售/三合一（含万能牌扫描规则）+ Action 校验 | T1 经济与商店用例全绿 |
| **M2 战斗与淘汰**（1 天） | 配对/战斗模拟/事件日志/伤害公式/平局/出局/名次；7 AI 跑完整局 | T4 首跑 100 局 0 异常 |
| **M3 CLI 试玩**（0.5 天） | 文本渲染 + 交互命令；手动玩通一局；数值微调 | T6 完成 |
| **M4 测试收官**（1 天） | T2/T3/T5 补齐；千局模拟；性能达标；修正一切不稳定项 | 全部验收标准达成 |

合计约 **4 个工作日**，之后地基完成，进入功能迭代（词条 → 装备 → 羁绊 → 元层 → 联机 → UI）。

---

## 13. 后续扩展路线（每个点都已预留）

| 阶段 | 内容 | 已预留的接口 |
|---|---|---|
| P2 词条系统 | 快手/遗言/成长/消灭/护盾/嘲讽/增伤/连续行动 | `bus.ts` 挂 trigger、`effects.ts` 注册 effect；卡表 `skills[]` 已就绪 |
| P3 装备系统 | 武器/道具/投掷物/消耗卡 + 4 槽位 | 卡表 `type` 字段；目标选择函数独立（按武器类型扩展）；KAC-变色龙规则已定义 |
| P4 羁绊 | 阵营 2/4 档激活 | 卡表 `faction` 字段；场上统计器后置 |
| P5 元层 | 法宝 4 选 1 / 战术升级 3 选 1 / Buff 回合 | 回合钩子插入三选一阶段；全局效果挂 bus |
| P6 PVE | 怪物来袭 + 牌魂 | 配对阶段插入怪物场次；战斗中获得卡不即时合并的规则已定 |
| P7 联机 | 8 人真匹配 | Action 上移服务端（状态同步）；确定性 + 回放已就绪 |
| P8 UI | 竖屏图形界面 | 渲染层替换 CLI；事件日志驱动动画 |
| P9 外围 | 段位/通行证/钻石赛/图鉴 | 独立于对局核心，随时可加 |

---

## 14. 待你拍板的点（不阻塞开工，全部是配置表替换）

> **数值速查与调整入口：见《当前数值.md》——你直接改那份 md 告诉我数值，我同步 config/。**

1. **全部角色卡的攻/血数值**：等你实机截图（含分裂者的攻血）
2. **商店概率完整分布**：绿/蓝/紫/金各等级占比等你提供（橙已定：L1~L3=0、L4=10%、L5=30%）
3. **升级经验表**：3→4、4→5 为占位值（12/16），等你的准确数值

以上数据到位前，框架用占位值开发，**到位于改配置表即可**。

---

## 15. 开发状态（2026-08-15 更新）

**M0~M4 全部完成 ✅，MVP 可玩。**

| 里程碑 | 状态 | 验收结果 |
|---|---|---|
| M0 脚手架 | ✅ | TS + vitest 工程、seedable RNG、GameState、配置表 + 校验 |
| M1 商店经济 | ✅ | 金币阶梯/经验/升级/商店/购买/出售/三合一（含万能牌扫描规则）/Action 校验 |
| M2 战斗与淘汰 | ✅ | 配对（不重复对手）/交替战斗/事件日志/饱和曲线伤害/平局/出局名次 |
| M3 CLI 试玩 | ✅ | 文本渲染 + 交互命令（`npm run sim`） |
| M4 测试收官 | ✅ | **71 测试全绿**：T1 单元 + T2 确定性 + T3 fuzz（1万条随机操作零污染）+ T4 千局 0 异常 + T5 回放一致 + T6 手动试玩 |

- **性能**：1000 局全 AI 模拟 749ms（**0.75ms/局**，验收标准 <100ms）
- **运行**：
  - `npm run web` —— **Web 游玩界面**（人类游玩主入口，竖屏布局 http://localhost:5173，`?seed=N` 可指定对局种子）
  - `npm run sim` —— CLI 交互模式（备用）
  - `npm run sim -- --auto --seed 42` —— 全 AI 观战/冒烟
  - `npm run sim -- --batch 100 --seed 1` —— 批量 100 局输出统计（**AI 测试主入口**：平均耗时/平均回合/夺冠分布）
  - `npm test` —— 全部测试
- **Git**：仓库已初始化，MVP 提交 `c665833`（后续每个功能迭代按约定提交）
- **架构落地备注**：AI 使用独立随机源（seed+playerId 派生），不消耗对局 RNG——回放时 Action 日志可原样重放（T5 已验证），此设计将来联机同样成立
- 开发中发现并修复的问题：出局名次计算顺序（先算存活数再置淘汰标志）、配对自我污染、回放 RNG 发散、`applyMove` 交换分支——全部被测试捕获

**下一步（P2 词条系统）**：快手/遗言/成长/消灭/护盾/嘲讽/增伤/连续行动，接入点已就绪（`bus.ts` trigger + `effects.ts` 注册表 + 卡表 `skills[]`）。

---

*本方案为最简框架设计，所有数值集中于 config/ 一处，任何一项拍板变更都只动配置表。*
