# AI 交接：LoL 自动战斗卡牌改造

## 用户已经确认的目标

在现有“悠悠牌”8 人自动战斗卡牌 MVP 上，按默认方案直接开发 LoL 英雄主题的本地可玩原型。不要重新询问需求；继续实施即可。

详细方案：`LOL改编实施方案.md`。

目标不是简单换皮，第一版要完成：

- 20 名英雄 + 1 张英雄复制器
- 5 个地区、6 个职业及 2/4 人羁绊
- 法力、主动技能、护盾、治疗、控制、多段攻击和目标选择
- 会组羁绊、会站位的基础 AI
- 显示技能、羁绊、法力和战斗事件的 Web UI
- 完整测试、确定性验证、1000 局模拟和生产构建

## 工作区

只在 `D:\Codex_app`（本仓库根目录）工作。旧目录 `D:\Claude_app\18_uucard_game`、`D:\Codex\_app` 都不要使用。

## 当前基线

TypeScript + Vite + Vitest 项目。**LoL 改编 + 第二轮优化 + 手机竖屏版式重做 + 开始/选图/战斗界面重做均已完成**：

- 24 个测试文件 / 297 个测试全部通过，`npx tsc --noEmit` 无错，`npm run web:build` 成功。
- 1000 局模拟：平均 9.6 回合、2.5ms/局，20 名英雄全部会出场，羁绊 lift 收敛到 1.00~1.36（加成长后诺克萨斯 0.92→1.07、射手 0.96→1.13），上阵卡带装备比例 73.3%。
- 20 名英雄 + 英雄复制器 + 13 张武器（装备）卡（含武器线金色万能牌）、5 地区 6 职业 2/4 羁绊、法力/技能/护盾/治疗/控制/多段攻击、评分制 AI 全部落盘。
- **完整手机竖屏流程**：开始界面（战绩 + 立绘墙）→ 匹配选图（3 张地图 · 5 秒倒计时 · 8 人投票，票数最多者生效）→ 对局界面。参考截图是 1080×2400 横存的文件，**要转 90° 才是正确的观察方向**。
- **版式 = 手机竖屏（9:20）**：桌面浏览器用 #phone（min(100vw, 100vh*0.45)）居中一块竖屏区域、两侧留黑边。行高比例 顶栏6/商店34/棋盘34/仓库22；右列宽 46px。商店=3 张**竖版卡横排** + 升级/刷新 + **浮在面板右下角的准备圆钮**；棋盘=**3 列 × 2 行**（第一行前排 1/2/3、第二行后排 4/5/6）；仓库=一条横滑槽（武器与英雄共用，武器排最前）；右列上半 8 名对手竖排（和商店同高）、下半羁绊竖排（和棋盘同高），各自竖滑。
- **战斗界面**：上下两块棋盘（对手在上/我方在下，双方前排都贴近中间的金色横分割线）+ 准星，阵亡单位盖「消灭」印记，当前行动者青色高亮。棋盘是 3 列 × 2 行 = 6 格（和参考图一致）。
- **弹窗**：轻点卡牌看详情（含二星攻血预览）· 轻点羁绊看阵营与档位效果 · 轻点对手头像看其上回合阵容 · 点等级牌看商店概率表 · 准备前看双方阵容与战力。
- 卡面立绘用 Riot Data Dragon 官方 `loading` 竖版图（308×560），三级回退：立绘 → 方形头像 → 首字占位。
- **卡面不写字**（对齐参考图）：棋盘/仓库卡只有立绘 + 角上攻血 + 底部武器槽 + 顶部细法力条；文字全在详情弹窗里（名字保留在 `img alt`）。棋盘卡是**竖版**（ATK 左下 / HP 右下），槽位 `overflow:hidden` 保证任何屏幕尺寸都不遮挡。
- 战斗打击表现：受击闪光/震屏/大号描边伤害数字/技能名横幅/切后排/时空回溯/击杀爆点，速度档 原速·2×·3×·跳过。
- 体验：轻点卡牌看详情、点等级牌看商店概率表、准备前看双方阵容预览与战力、羁绊激活提示、拖动可落位高亮。

改造前的原始基线（供对照）：12 个测试文件、91 个测试通过。旧卡（`loey/lan/longye/fenliezhe` 等）保留在 `config/cards.ts` 的 `LEGACY_CARDS`，不进商店。

## 玩法规则（2026-09 用户反馈批次）

- **开局赠礼**：每人开局 **1 个绿色英雄直接上阵在 1 号位 + 1 件绿色武器进武器库**（省得新手不知道要拖）。
  由 `createGame` 里的 `grantStartingLoadout` 发放，用**独立派生的 RNG**（`seed ^ 0x5eed1a7e`），不动主 RNG 序列。
  测试要干净棋盘时传 `createGame(seed, null, { startingLoadout: false })`。
- **初始金币 2**：`economy.goldStart = 2`，第 1 回合 `beginRound` 发放。
- **成长属性**：部分英雄（德莱厄斯/德莱文/金克丝 `growth:{atk:1}`、斯维因 `growth:{hp:3}`）
  **上阵期间每回合**永久涨攻/涨血；拿下场不清零也不再涨；三合一累加。
  数值存在卡实例的 `growthAtk/growthHp`，`totalAtk/totalHp` 已计入，卡面右上角有绿色角标、详情里有成长说明。
  加成长的正是当时最弱的诺克萨斯与射手，属于**有数据支持的补强**。
- **出售区**：拖动自己已有的卡/武器时，商店面板上盖一层半透明「出售区」+ 回收价（`core/shop.ts` 的 `sellPriceOf`），
  悬停时点亮，松手移除。遮罩必须 `pointer-events:none`，否则会挡住 `elementFromPoint` 的落点判定。
- **分辨率：固定 9:20（已回退，不要再改）**。曾经试过"自适应分辨率"（宽度在视口宽/500px/高度×0.68 里取值），
  用户反馈界面出问题，已**整套回退**成 `--aspect: 0.45` + `#phone { width: min(100vw, 100vh*aspect) }`。
  ⚠️ **教训**：那次改动把 `--aspect` 改名成了 `--min-aspect`/`--max-aspect`，
  但 `.battle-root` 仍在用 `var(--aspect)` —— 变量不存在 → `min()` 整条失效 → **战斗结算面板挤成中间一小坨**。
  改尺寸相关的 CSS 变量前，先 `Select-String -Path web/style.css -Pattern 'var\(--aspect'` 查全部引用点。
  自检：`npm run probe:viewport`（7 种尺寸量游戏区并查溢出）、
  `npm run probe:battle`（量 .battle-root 是否铺满、格子是否正常）。
- **最后一局必须播完战斗**：`finishGameIfNeeded()` 只能在战斗视图点完【继续】之后调用。
  之前为了防卡死把它提到开战前，导致"最后一局直接结算、看不到自己那场战斗"（用户反馈）。
## 已部署（线上可玩）

- **https://adv1346288207.github.io/lol-autobattler/** ← `git push` 就会自动重新发布
- 仓库：https://github.com/adv1346288207/lol-autobattler （public）
- 流水线 `.github/workflows/deploy.yml`：校验素材在库 → `npm test` → 构建 → 发布 Pages
- **素材已入库**（53 个文件约 1.6 MB），构建自包含、CI 不需要联网取图；`splashes/`/`spells/` 仍不入库
- `vite.config.ts` 的 `base` 默认 `"./"`，`web/ui.ts` 的 `assetUrl()` 补 `BASE_URL`，
  所以同一份产物放在 `/<repo>/` 子路径下也能用（这是部署前必须修的坑，否则 55 张图全 404）
- Pages 站点**需要手动开一次**（`GITHUB_TOKEN` 没权限建站点）：
  `gh api -X POST repos/<user>/<repo>/pages -f build_type=workflow`
- 验证线上是否真的能玩：
  `npm run play -- --url https://adv1346288207.github.io/lol-autobattler --rounds 3`
  （脚本会把任何 4xx/5xx 报出来，所以能证明线上素材真的加载成功）
## 自己玩一局（自走自检，重要）

不要只靠代码感觉改 UI。用 Playwright 驱动真实浏览器自己玩：

```powershell
npm run web:build
npm run web:preview -- --port 4188      # 后台起静态服务
npm run play -- --rounds 3 --seed 7     # 自动玩 3 回合，逐步截图 + 报运行时报错
```

- 脚本在 `scripts/play-web.mjs`，用 `playwright-core` 驱动本机已装的 Chrome（不下载浏览器）。
- 它会走完整流程：开始界面 → 开始匹配 → 地图投票 → 每回合【买卡→上阵→装备武器→准备→开战→继续】，
  并在每步截图到 `shots/`（`shots/report.txt` 是步骤清单 + 报错汇总）。
- 它会捕获 `console.error` / `pageerror` / **HTTP 4xx-5xx** / **unhandledrejection**；
  有任何一条就以非零码退出，可以当回归门禁用。
- 已经靠它抓到并修掉：战斗播放层没有 try/catch 会把玩家卡死、【继续 ▶】永远不出现；favicon 404；
  商店卡不撑满面板留下大片空白。
- 自检脚本内置断言：图鉴点角色详情必须真的在最上层（`elementFromPoint` 命中判定）、图鉴页签数量、
  设置面板含退出项、详情里卸下武器回到仓库、卖带装备英雄金币/武器至少一项增加、
  准备按钮禁用时必须已弹终局面板（防卡死）、拖武器必须出现拖拽幽灵。4 个种子连跑 16 回合全绿。
- 其他靠自检/读码抓到并修掉的真 bug：
  6. **地图投票 8 个人永远投同一张**：派生式里的 `pid * 40503`，而 40503 = 3 × 13501 能被 3 整除，
     取模 3 后跟 pid 无关。现改用 murmur 风格整数混合（`deriveMapVote`，全程 Math.imul）。
  7. **拖武器的没有拖拽幽灵**：幽灵查找选择器写的是 `.weapon-chip`，而仓库武器类名早已改成 `.bench-weapon`；
     并且幽灵显式设置了宽高（武器芯片是 height:100% + aspect-ratio，克隆到 body 下算不出大小）。
  8. **拖动后松手会误弹详情**：按下和抬起落在同一元素时浏览器会补发 click，把卡拖出去又拖回原处就会弹详情。
     现在真拖动会 `suppressTap()`，320ms 内的 click 一律忽略。
## Web 界面预览参数（开发/截图用）

```bash
npm run web                                     # 正常游玩（开始界面 → 选图 → 对局）
http://localhost:5173/?quick=1                  # 跳过开始界面与选图直接开局
http://localhost:5173/?screen=match             # 直接看匹配选图
http://localhost:5173/?seed=7&skip=6            # 先自动打 6 回合，看中后期界面
http://localhost:5173/?seed=7&skip=6&battle=1   # 直接播放一场战斗
http://localhost:5173/?panel=trait:demacia      # 直接打开羁绊详情（opp:3 / card:ahri / odds 同理）
http://localhost:5173/?w=360                    # 模拟 360px 窄屏
```

用无头 Chrome 截图验证 UI 的方法（不依赖任何 npm 依赖）：

```powershell
npm run web:build
npm run web:preview -- --port 4188     # 后台起静态服务
& "C:\Program Files\Google\Chrome\Application\chrome.exe" --headless=new --disable-gpu `
  --hide-scrollbars --user-data-dir="$env:TEMP\chr" --window-size=504,900 --virtual-time-budget=6000 `
  --screenshot="D:\shot.png" "http://localhost:4188/?seed=11&skip=6"
```

注意：无头 Chrome 会把视口宽度钳制在 ~504px，所以窄屏要用 `?w=` 参数模拟；`--dump-dom` 可用来断言战斗中确实生成了浮字/横幅等特效元素（CSS 动画帧在无头截图里不会保留）。

## 必须保护的现有修改

复制时 Git 状态：

```text
 M core/economy.ts
 M web/ui.ts
?? LOL改编实施方案.md
?? start-ai.bat
?? start-cli.bat
?? start-web.bat
```

- 这些修改属于用户/已有工作，不得回退。
- 禁止 `git reset --hard` 和 `git checkout -- <file>`。
- 修改前先看 `git diff -- core/economy.ts web/ui.ts`，用小补丁合并。
- 未经用户要求不要自行提交 commit。

## 修正后的默认英雄池

每名英雄有 1 个地区、1～2 个职业标签。方案初稿中的锐雯改为德莱文，保证射手羁绊人数足够。

| 地区 | 英雄（品质；职业） |
|---|---|
| 德玛西亚 | 盖伦（绿；先锋/战士）、波比（蓝；先锋）、拉克丝（紫；法师/辅助）、嘉文四世（橙；战士/辅助） |
| 诺克萨斯 | 德莱厄斯（绿；战士）、德莱文（蓝；射手）、卡特琳娜（紫；刺客）、斯维因（橙；法师） |
| 艾欧尼亚 | 慎（绿；先锋/辅助）、亚索（蓝；战士/刺客）、阿狸（紫；法师）、艾瑞莉娅（橙；战士/刺客） |
| 皮尔特沃夫/祖安 | 蔚（绿；先锋/战士）、金克丝（蓝；射手）、凯特琳（紫；射手）、艾克（橙；刺客/法师） |
| 暗影岛 | 赫卡里姆（绿；先锋/战士）、卡莉丝塔（蓝；射手）、锤石（紫；辅助）、佛耶戈（橙；刺客） |

万能牌改名“英雄复制器”，保留分裂者的合成补齐规则，不参与羁绊。

## 实施顺序

### A. 先保护基线

运行：

```text
git status --short
git diff -- core/economy.ts web/ui.ts
npm test
npm run web:build
```

基线若不是 91 测试通过，先诊断，不能用覆盖文件的方式规避。

### B. 英雄数据

1. 扩展 `config/cards.ts`：地区、职业数组、图片路径、法力和主动技能配置。
2. 正式 `CARD_POOL` 改为上述 20 名英雄 + 英雄复制器。
3. 旧测试大量引用 `loey/lan/longye/fenliezhe`。把旧卡放入 `LEGACY_CARDS` 并继续加入 `CARD_BY_ID`，但不要让旧卡进入正式商店 `CARD_POOL`。
4. 更新 `config/validate.ts`。

### C. 通用战斗引擎

扩展状态/战斗快照：`maxHp`、`mana`、`maxMana`、`startMana`、`shield`、`statuses`、`traits`、攻击计数、技能倍率。

建议新增：

- `core/targeting.ts`：前排、后排、最低生命、当前目标、整列、全体和友方目标。
- `core/effects.ts`：伤害、治疗、护盾、眩晕、攻击增益、多段攻击、斩杀和破盾。
- `core/traits.ts`：地区/职业统计与效果。
- `core/battle.ts`：法力、施法、状态、死亡和羁绊触发。

新增事件：`SKILL_CAST`、`DAMAGE`、`HEAL`、`SHIELD_GAIN`、`SHIELD_BREAK`、`STATUS_APPLY`、`STATUS_REMOVE`、`MANA_CHANGE`、`TRAIT_TRIGGER`。

必须保持：战斗只修改棋盘快照，不污染持久棋盘；同 seed + 同操作序列结果一致。

先实现 4 名代表英雄验证通用能力：

1. 盖伦：全体伤害 + 自身护盾。
2. 拉克丝：后排伤害 + 友方护盾。
3. 阿狸：后排伤害 + 眩晕/魅惑。
4. 卡莉丝塔：多段攻击。

通用引擎稳定后再用配置组合其余 16 名技能，避免 20 份硬编码。

### D. 羁绊

地区默认效果：德玛西亚开战护盾；诺克萨斯攻击/击杀成长；艾欧尼亚初始法力/施法回蓝；皮城祖安确定性强化友军；暗影岛友方死亡后强化存活友军。

职业默认效果：先锋生命/护盾；战士攻击/低血减伤；法师技能倍率/回蓝；射手按固定攻击次数追加攻击；刺客切后排/强化首次技能；辅助强化治疗和护盾。

数值必须配置化，不要散落在 UI 或战斗主循环。

### E. AI 与 UI

AI 评分应考虑基础强度、三合一、羁绊升级、阵容缺口和阶段；先锋/战士优先前排，射手/法师/辅助优先后排。AI 继续使用独立 RNG。

在保留 `web/ui.ts` 已有修改的基础上增加地区/职业标签、技能说明、头像占位、羁绊面板、法力/护盾条和技能事件。保留现有拖动购买、上阵交换、出售、准备、加速和跳过。

### F. 测试与验收

新增目标选择、法力施法、护盾、治疗、控制、多段攻击、羁绊阈值、代表英雄、新卡池合成、确定性和 1000 局模拟测试。

最终必须通过：

```text
npm test
npx tsc --noEmit
npm run web:build
```

## IP 与资源边界

当前定位是本地个人试玩原型。不要主动下载或提交来源不明的 LoL 立绘、Logo、图标和音效；先使用 CSS、文字和原创占位图。若用户以后要求公开发布或商业化，先提醒确认 Riot/Tencent 授权并准备原创替换方案。

## 交接结论

- LoL 改编的阶段 A~G、第二轮优化（平衡 + 视觉 + 体验）、以及**手机竖屏版式重做**均已完成并落盘。
- 当前数值、平衡 lift 数据、版式说明与调优依据记录在 `当前数值.md`。
- 平衡方法：`npm run sim -- --batch 1000` 会输出每个英雄/羁绊的"前四率 lift"；调优时**只改有数据支持的项**。
  昂贵羁绊（法师/刺客）的 lift 天然偏高，因为只有打到 4/5 级商店的玩家才凑得出来，属于选择偏差，不要据此削弱。
- 资产：`npm run assets:lol` 下载 20 张方形头像 + 20 张卡面立绘（loading 竖版图，UI 卡牌正面），
  清单见 `web/public/assets/lol/manifest.json`，**图片已入库**（构建自包含），说明与版权注意事项见同目录 `README.md`。
- **武器 = 装备（不是上阵单位）**：进商店买卖、3 张同名合成二星，然后**装备到英雄身上**（每名英雄 **2 格**，装满后自动替换较弱的一件）。
  `CardInstance.atk/hp` 只存英雄自身数值，武器放 `CardInstance.equips`，面板数值一律用
  `totalAtk()/totalHp()` 读取；武器库存是 `PlayerState.weapons`（上限 8）。
  金武器是武器线万能牌（2 真 + 1 金，或 1 真 + 2 金都能合成），两条万能线互不通用。
  商店内英雄:武器 = 3:1 加权抽取。1000 局里 57.1% 的上阵卡带着装备（每名英雄 2 个武器槽）。
  UI 入口：拖武器到英雄卡装备 / 英雄详情里【卸下武器】/ 拖武器到商店出售；AI 会自动装备并在明显更强时替换。
  蓝卡及以上的武器各带 1 条战斗被动（开战加成 / 首次击杀成长 / 首次施法成长 / 首次致命伤回溯），
  带被动的武器同步下调了基础攻血。
- **图鉴（顶栏 ▤）**：全部/角色/武器 页签 + 品质筛选；**设置（顶栏 ⚙）**：可退出到主菜单。
- 已知未尽事项：
  1. 最弱羁绊是诺克萨斯（lift 0.92）与射手（0.96）；两者都需要英雄侧补强，而不是削装备。
  2. 战斗动画只在真实浏览器里验证过 DOM 结构（浮字/横幅/爆点元素确实生成），无头截图不保留 CSS 动画帧，**动效的实际观感仍需要人眼过一遍**。
  3. 版式已按手机竖屏对齐，但**字体、动效节奏、技能图标等细节美术**与商业产品仍有差距（见 `当前数值.md` 第 10 节）。
  4. 每名英雄固定 2 个武器槽（不可扩展）；绿卡与金色万能牌按需求保持纯数值、没有被动。
- 未经用户要求不要创建 commit。
